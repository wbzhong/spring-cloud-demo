spring:
  application:
    name: config-client
  cloud:
    bus:
      trace:
        enabled: true
      enabled: true
server:
  port: 8517

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh